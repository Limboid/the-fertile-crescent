def infer_modalities(ds, env, spaces, description, quiet=False):
    """Infers the modalities of a dataset.
    Uses a text classifier to break ties
    Asks user for confirmation if necessary."""
    pass

# maybe bring tree ops here from utils?
# no, because that's not a convenience function
# yes, because downstream developers may need it
# but utils still feels like a better place for it
